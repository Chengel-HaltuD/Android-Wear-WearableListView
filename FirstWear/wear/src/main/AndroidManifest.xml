<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.gele.wear.firstwear">
    <uses-feature android:name="android.hardware.type.watch" />
    <application android:allowBackup="true" android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher" android:theme="@android:style/Theme.DeviceDefault">

        <activity
            android:name=".WearActivity"
            android:exported="true"
            android:allowEmbedded="true"
            android:taskAffinity=""
            android:label="MyRunningApp"
            android:theme="@android:style/Theme.DeviceDefault.Light">

            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="com.google.android.voicesearch.SELF_NOTE" />
            </intent-filter>

        </activity>

        <activity  android:name="com.gele.wear.firstwear.AdvancedListActivity"></activity>
        <activity  android:name="com.gele.wear.firstwear.SimpleListActivity"></activity>

    </application>

</manifest>
